{
  "name": "pharmascope",
  "version": "1.0.0",
  "description": "Pediatric prescription checking AI SaaS",
  "private": true,
  "scripts": {
    "dev": "cd frontend && npm run start",
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start",
    "test": "cd frontend && npm test",
    "backend": "cd backend && python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
